# Design — field-derived orchestrate routing findings

## Decision

Keep executable-evidence repair as a provisional cross-cutting rule rather than
adding a routing-table area. Execution access selects the capable worker for a
special case; a new area would create a competing task classification and imply a
new benchmarked interface.

## Reconciliation

The current tree makes `review-routing.md` the sole reviewer-precedence
authority. The change records the rendered-evidence and Codex-only limits in the
routing table and Codex-parent dispatch reference, while explicitly deferring
selection precedence to that existing authority.

## Deferred measurement

Sol's planted-defect review-fixture replay remains owed. The field observations
are insufficient to promote Sol to a Codex-only load-bearing review rung.
