# Tasks

- [x] Update the payload wording in both `SKILL.md` grants and frontmatter
      descriptions, keeping each description at or under the 1024-byte cap.
- [x] Update both `agents/openai.yaml` default prompts and the three adapter
      approval rationales.
- [x] Restate the grant at the four coordinator reviewer-dispatch steps.
- [x] Extend the payload pin in `tests/test_behavior.py`, red first.
- [x] Run OpenSpec validation and the repository gate.
