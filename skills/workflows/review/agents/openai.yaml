interface:
  display_name: "Review"
  short_description: "Route to one review type: code, plan, personas, or security"
  default_prompt: "Use $review to route this to the right review skill — code, plan, personas, or security."
